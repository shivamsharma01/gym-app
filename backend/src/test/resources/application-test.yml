# Test profile: do not background-dispatch outbox commands — tests drive dispatch/results
# deterministically. The in-process simulator is also off so we never fabricate hardware success.
app:
  gateway:
    simulator-enabled: false
    outbox:
      dispatcher-enabled: false
  rate-limit:
    enabled: false
