package com.example.gym.device.domain;

/** Physical placement of a device, used to resolve attendance direction (entry vs exit). */
public enum DeviceRole {
    ENTRANCE,
    EXIT,
    UNSPECIFIED
}
